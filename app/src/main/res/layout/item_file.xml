<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/fileIcon"
        android:layout_width="50dp"
        android:layout_height="50dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/fileName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:gravity="start"
        android:maxLines="2"
        android:textColor="?attr/primaryTextColor"
        android:textSize="?attr/primaryTextSize"
        app:layout_constraintEnd_toStartOf="@id/fileSelectedCheckBox"
        app:layout_constraintStart_toEndOf="@id/fileIcon"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="File Name  wejn oj wef owen olejwfojw efnd fnoewnjofn.pdf" />

    <TextView
        android:id="@+id/fileSize"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="start"
        android:textColor="?attr/secondaryTextColor"
        android:textSize="?attr/secondaryTextSize"
        app:layout_constraintEnd_toEndOf="@id/fileName"
        app:layout_constraintStart_toStartOf="@id/fileName"
        app:layout_constraintTop_toBottomOf="@id/fileName"
        tools:text="size | time" />

    <androidx.appcompat.widget.AppCompatCheckBox
        android:id="@+id/fileSelectedCheckBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>